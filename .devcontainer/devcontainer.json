{
  "name": "zmail",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "cd node && npm install",
  "forwardPorts": [3000],
  "remoteEnv": {
    "ZMAIL_HOME": "${workspaceFolder}/.zmail"
  }
}
